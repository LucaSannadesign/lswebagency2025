---
/**
 * Background globale del sito.
 * Versione semplificata: niente gradient rosa,
 * solo bianco (o scuro in dark mode).
 */
const { class: className = '' } = Astro.props;
---

<div
  aria-hidden="true"
  class={`pointer-events-none absolute inset-0 -z-10 bg-white dark:bg-slate-950 ${className}`}
/>